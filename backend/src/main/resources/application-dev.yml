spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ai_learning_platform_dev?useSSL=false&serverTimezone=UTC&characterEncoding=utf8
    username: root
    password: password

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

server:
  port: 8080